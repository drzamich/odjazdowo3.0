import 'reflect-metadata';

import { Mock } from 'moq.ts';
import { ZtmScrapeService } from '../service';

describe(ZtmScrapeService.name, () => {
  it('smoke test', () => {
    expect(true).toBeTruthy();
  });
});
